# | export
def process_v1_search_users(v1_user_ls: list[dict] # response from 
) -> list[dict]:

    clean_users = []

    for obj_user in v1_user_ls:
        clean_users.append( {
            "id": dd_user.id,
            "displayName": dd_user.displayName,
            "roleId": dd_user.roleId,
            "userName": dd_user.userName,
            "emailAddress": dd_user.emailAddress,
        })
